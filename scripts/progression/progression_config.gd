class_name ProgressionConfig
extends Resource

@export var shop_unlock_threshold: int = 50
## FP earned per hit during autoplay, as a fraction of the active play rate.
@export var autoplay_friendship_point_rate: float = 0.5
